Hello every one, this project is aimed to study automake GNU in linux platform.

Now try to generate configure file by autoconf command. Run these command step by step.

aclocal
autoconf
automake --add-missing